const MAX_VALUE = 100;

const student = {
    ssn : 10,
    name : "김기정",
};

const fruits = ["사과", "포도", "딸기"];

const sum = function(x, y){
    return x + y;
}

// 한방에 객체에 담아서 내보내기
export {
    MAX_VALUE,
    student,
    fruits,
    sum
};